# DESIGN — 邮你前行-地戏新生

## 1. 设计方向

网站采用“编辑式文化档案”风格：像一册经过整理的田野笔记、地方文化专刊或小型展览图录，而不是商业首页。视觉以暖纸色、墨色文字、朱红批注与真实田野摄影构成，强调阅读节奏、史料来源和人物尊严。

关键词：**纸本、田野、档案、口述、克制、温度、时间痕迹**。

## 2. 设计原则

1. **内容先于装饰**：图片、引语与档案字段承担信息，装饰只负责建立层级。
2. **真实环境优先**：不把面具抠成脱离环境的商品图；保留墙面、木架、工坊与戏台语境。
3. **人物优先**：人物照片、姓名、身份和第一人称引语形成页面重心，不把受访者包装成营销案例。
4. **编辑式节奏**：使用章节编号、小标、长短段落、跨栏图片、引语与留白交替，而非重复卡片墙。
5. **朱红克制使用**：强调色用于导航状态、章节标识、链接、焦点和少量数据，不铺满大面积背景。
6. **信息不确定性可见**：未知角色直接显示“待考证”，不使用视觉暗示制造确定性。
7. **动效从属阅读**：只使用轻微进入与状态变化；减少动态效果偏好下立即呈现内容。

## 3. 色彩 Tokens

实现必须使用以下全局变量：

```css
:root {
  --bg: #F5F4ED;
  --bg-alt: #EFEDE4;
  --ink: #1A1A17;
  --ink-soft: #4A4842;
  --accent: #C0392B;
  --accent-2: #C96442;
  --accent-3: #2F4858;
  --line: #D8D4C8;
}
```

| Token | 角色 | 使用规则 |
| --- | --- | --- |
| `--bg` | 暖纸主背景 | 页面主体、深色媒体上的反色文字 |
| `--bg-alt` | 次级纸色 | 章节交替、弱提示背景、占位背景 |
| `--ink` | 主墨色 | 标题、正文、核心数据 |
| `--ink-soft` | 次墨色 | 图注、元数据、辅助说明 |
| `--accent` | 朱红 | 链接、当前状态、章节线、焦点、主按钮 |
| `--accent-2` | 陶红 | 悬停与次级强调 |
| `--accent-3` | 靛灰蓝 | 低频辅助色，不与朱红争夺主层级 |
| `--line` | 纸面分隔线 | 边框、时间轴、章节分隔 |

颜色不得成为传达筛选状态、可访问状态或授权状态的唯一手段。

## 4. 字体与排版

### 字体栈

```css
--font-serif: "Noto Serif SC", "Source Han Serif SC", "思源宋体", "Songti SC", "SimSun", serif;
--font-sans: "Noto Sans SC", "Source Han Sans SC", "思源黑体", "PingFang SC", "Microsoft YaHei", sans-serif;
```

- 标题、引语与首字下沉使用宋体栈，建立文化专刊气质。
- 正文、导航、按钮和元数据使用黑体栈，保证屏幕可读性。
- 不依赖远程字体服务，避免加载阻塞与隐私外联。

### 字号层级

```css
--fs-display: clamp(2.4rem, 5.2vw + 1rem, 4.2rem);
--fs-h1: clamp(2rem, 3.6vw + 0.8rem, 3rem);
--fs-h2: clamp(1.55rem, 2.4vw + 0.6rem, 2.15rem);
--fs-h3: clamp(1.2rem, 1.4vw + 0.5rem, 1.45rem);
--fs-body: 1.0625rem;
--fs-small: 0.875rem;
--fs-meta: 0.8125rem;
```

- 正文行高约 `1.85`，适合中文长文。
- 正文最大行宽 `68ch`；常规容器最大宽度 `1180px`。
- 标题允许平衡换行，正文不做两端强制对齐。
- 元数据可增加字距，但长正文不使用宽字距。
- 引语必须保留明确来源，不用无署名的装饰性金句。

## 5. 间距与形状

```css
--container: 1180px;
--measure: 68ch;
--space-xs: 0.5rem;
--space-s: 0.875rem;
--space-m: 1.5rem;
--space-l: 2.75rem;
--space-xl: 4.5rem;
--space-xxl: 7rem;
--radius: 4px;
```

- 章节之间使用大留白，不靠高饱和色块切割。
- 圆角只用于轻微消除屏幕锐利感，禁止大圆角胶囊式卡片墙。
- 胶囊外形只允许用于短标签或数字口径，不用于所有容器。
- 阴影尽量不用；层级以边线、纸色变化和留白建立。

## 6. 页面构图

### 首页

八个叙事章节按“引入—缘起—任务—路线—人物—档案—面具—团队”展开。首屏使用真实地戏表演图片与非居中的编辑式标题，避免“居中口号 + 三张功能卡”的商业模板结构。

### 列表页

- 人物列表采用图文交错编排，避免七张同构卡片。
- 成果列表以编号、类型与正文横向组织，不做数据仪表盘。
- 档案入口可使用三个结构清晰的入口，但视觉上保持图录感而非产品功能卡。

### 详情页

- 人物详情按肖像、身份、关系、故事、引语、相关影像、相关面具、访谈信息、来源署名组织。
- 面具详情突出大图和档案字段；角色未知时显示“待考证”。
- 图片说明统一包含必要的时间、地点或来源，不重复替代文本。

## 7. 图像与视频

### 图片

- 构建期生成 AVIF/WebP，并保留合适的 JPEG 回退。
- 使用响应式 `srcset`/`sizes`，首屏关键图可预加载，其余懒加载。
- 人物照片不做夸张滤镜；面具照片保留真实色彩与陈列环境。
- 替代文本描述画面内容，不以“图片”开头，不把图注机械复制为替代文本。

### 视频

- 所有视频使用原生控件、海报和 `preload="none"`。
- 禁止自动播放和自动出声。
- 页面只发布 20–60 秒的处理后代表片段；完整原始视频不进入发布目录。
- 海报应能识别场景或人物，避免全黑帧、遮挡帧与无意义运动模糊。

## 8. 组件规范

### 导航

- 桌面导航固定在纸色页眉中，当前栏目同时使用文字/下划线或边线状态。
- 移动菜单按钮至少 `44 × 44px`，维护 `aria-expanded`。
- 提供跳至正文链接，页面变化后当前导航使用 `aria-current="page"`。

### 按钮与链接

- 主要按钮为朱红底纸色字，次按钮为透明底朱红边。
- 所有可交互目标最小高度 44px。
- 正文链接保留下划线，不仅依靠颜色表示可点击。
- `:focus-visible` 使用清晰的 2px 朱红轮廓与偏移。

### 面具筛选

- 使用原生 `<button>`，通过 `aria-pressed` 表示状态。
- “全部 / 墙面陈列 / 工坊近景”文字始终可见。
- 筛选后不改写档案数据，只改变当前显示集合。

### 灯箱

- 使用原生 `<dialog>`；支持关闭、前一张、后一张、方向键、Esc 与焦点恢复。
- 深色背景保证照片辨识度，控制按钮有文字或可访问名称。
- 禁止把灯箱作为查看档案字段的唯一入口。

## 9. 响应式规则

- **手机**：单栏优先，标题缩放，图文交错回落为自然阅读顺序，导航折叠。
- **平板**：根据内容长度保留一栏或两栏；不为维持桌面构图压缩正文。
- **桌面**：容器最大 1180px，正文仍限制在 68ch；图片可跨出正文栏形成编辑节奏。
- 任意断点不得产生横向滚动；长路径、邮箱与元数据可安全换行。

## 10. 动效与可访问性

- 进入动效只改变轻微位移与透明度，持续时间短，不串联复杂时间线。
- `prefers-reduced-motion: reduce` 下关闭平滑滚动、进入动画和非必要过渡。
- 语义顺序与视觉顺序保持一致，不能靠 CSS 重排改变阅读逻辑。
- 图片、视频、按钮、筛选与灯箱均支持键盘使用。
- 不在自动出现的浮层中夺取焦点；关闭对话框后将焦点返回触发元素。
- 对比度、字号、焦点和触控尺寸以 WCAG 2.2 AA 为最低目标。

## 11. 明确禁止的视觉模式

- SaaS 仪表盘、定价页、客户 Logo 墙、增长转化语言；
- 大面积玻璃拟态、霓虹渐变、厚重投影或 3D 卡片；
- 每个区块都套圆角白卡；
- 自动轮播、滚动劫持、视差背景或音频自动播放；
- 用虚构数据、无来源引语或示意头像填满空缺；
- 将未成年人塑造成猎奇或营销传播素材；
- 为视觉整齐裁掉档案所需的实体标签和环境信息。

## 12. 实现基准

设计基准实现位于 `src/styles/global.css`，页面组件应优先复用这些 tokens 与全局工具类。新增样式必须解释新的语义需求，避免在单页中复制近似颜色、字号与间距值。
